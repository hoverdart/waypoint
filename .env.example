# Docker Compose only. Copy to .env before running `docker compose up --build`.
POSTGRES_USER=waypoint
POSTGRES_PASSWORD=waypoint
POSTGRES_DB=waypoint_dev
POSTGRES_PORT=5433
BACKEND_PORT=8000
FRONTEND_PORT=3000
